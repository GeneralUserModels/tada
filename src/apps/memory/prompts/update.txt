Current date and time: **{now}**

Mode: `{mode}`

{update_rules}

{shared_wiki_rules}

{shared_source_rules}

## Inventory Plan

```json
{inventory_json}
```

## Existing Content Page Metadata

{existing_page_metadata}

## New Inputs Detected

{new_inputs_list}

## Task

Execute the inventory plan. Read the planned sources and existing pages. Return page operations for normal content pages with grounded facts, insight, confidence scores, and cross-links.

Important:
- Read an existing page before editing it.
- Prefer improving existing pages over creating near-duplicates.
- Create a page when a real entity, project, person, theme, or pattern has enough evidence for a useful paragraph.
- Create pages for newly discovered grounded entities even when inventory did not list them upfront.
- If you add a `[[wiki-link]]` to a new entity, also create or update the target page unless an existing page or index entry already represents it.
- If new information contradicts an existing page, update the page, lower confidence where needed, and preserve the older claim only when it remains historically useful.

Do not write files directly. Python will apply the returned operations after validating paths and frontmatter.

End with a single fenced JSON block. It must be the last thing in your final message.

```json
{{
  "create_pages": [
    {{
      "path": "relative/page.md",
      "markdown": "---\\ntitle: Page Title\\nconfidence: 0.7\\nlast_updated: YYYY-MM-DD\\n---\\n\\nPage body."
    }}
  ],
  "update_pages": [
    {{
      "path": "existing/page.md",
      "markdown": "---\\ntitle: Existing Page\\nconfidence: 0.8\\nlast_updated: YYYY-MM-DD\\n---\\n\\nFull replacement markdown."
    }}
  ],
  "notes": "Concise summary of pages created, pages updated, and unresolved catalog/log/schema issues."
}}
```
