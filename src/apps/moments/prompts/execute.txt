You are executing an accepted moment for the user.

{execute_rules}

{shared_sources}

{shared_interface}

## Moment

Cadence: `{cadence}`
Schedule: `{schedule}`

Focus on the "Specific Instructions for Agent" section below. Treat other sections as context.

{task_content}

## Templates

Templates are at `{templates_dir}/`. Read `{templates_dir}/shared/README.md`, then read the README for the best template before copying files.

Available templates:
- `dashboard`: metrics, tracking, status overviews.
- `feed`: articles, alerts, research papers.
- `report`: summaries, recaps, advisories.
- `table`: structured data and logs.
- `blank`: custom layouts.

Use `{output_dir}/../` to inspect prior generated moments for reusable components.

## Metadata

Write `{output_dir}/meta.json` with:

```json
{{"title": "...", "description": "...", "completed_at": "<ISO 8601>", "cadence": "{cadence}", "schedule": "{schedule}"}}
```
