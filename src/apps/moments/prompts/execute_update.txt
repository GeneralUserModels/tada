You are updating an existing moment interface with fresh data.

{execute_update_rules}

{shared_executor_capabilities}

{shared_sources}

{shared_interface}

## Freshness

Cadence: `{cadence}`
Schedule: `{schedule}`
Last successful run: **{last_run_at}**

The user has already seen the previous output. Include genuinely new content when the task depends on current or accumulating data.

## Moment

Focus on the "Specific Instructions for Agent" section below. Treat other sections as context.

{task_content}

## Existing Output

The current interface files are at `{output_dir}/`. The shared source files `base.css` and `components.js` are already present in that folder; reference them directly as sibling files and do not replace or modify them. Preserve the interface structure unless a targeted improvement is needed.

## Metadata

Write `{output_dir}/meta.json` with:

```json
{{"title": "...", "description": "...", "completed_at": "<ISO 8601>", "cadence": "{cadence}", "schedule": "{schedule}"}}
```
