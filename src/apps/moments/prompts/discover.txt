Current date and time: **{now}**

Mode: `{mode}`
Last discovery: **{last_discovery_date}**

{discover_rules}

{shared_sources}

{shared_moments}

## Existing Accepted Moments

{accepted_moments}

## Feedback And State Signals

{feedback_state_summary}

## Current Draft State

{draft_context}

## Activity Chunk Metadata

{chunk_metadata}

## Activity Chunk

{activity_chunk}

## Task

You are the creative ideation step for one chronological activity chunk. Use this chunk to propose useful moment ideas.

Return idea cards only. Do not produce patch operations, do not maintain draft state, do not promote moments, and do not write accepted markdown files. A separate compiler step will convert your ideas into draft candidates.

Favor specific ideas that produce concrete artifacts. It is fine to be creative here, but every idea must cite evidence from the activity chunk or targeted source context.

For `relation_to_existing`, use:
- `new` for a distinct new idea.
- `possible_update` when the idea may refresh an accepted moment or current draft.
- `duplicate` when it looks covered already.
- `weak` when it is probably not useful enough.

End with a single fenced JSON block. It must be the last thing in your final message.

```json
{{
  "ideas": [
    {{
      "title": "Short useful idea title",
      "topic_hint": "kebab-or-plain-topic",
      "artifact": "Concrete artifact the executor should produce",
      "why_useful": "Why this would save time, reduce missed information, clarify a decision, or make recurring work easier",
      "evidence": ["Evidence item with source path, line/timestamp, or other concrete pointer"],
      "source_paths": ["relative/source/path"],
      "cadence_hint": "once",
      "relation_to_existing": "new"
    }}
  ],
  "notes": "Short note about searches, files, or reasoning that informed the ideas."
}}
```
